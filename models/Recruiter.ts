export interface Recruiter {
  id: string;
  company: string;
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  jobTitle: string;
  createdAt: Date;
  userId: string;
}

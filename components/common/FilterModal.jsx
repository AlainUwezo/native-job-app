import { Text } from "@rneui/themed";
import { Modal, View } from "react-native";

const FilterModal = () => {
  return (
    <Modal>
      <View>
        <Text>Filter modalc</Text>
      </View>
    </Modal>
  );
};

export default FilterModal;
